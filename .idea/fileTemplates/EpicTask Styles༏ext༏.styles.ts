/**
 * ${NAME}
 * Created by ${USER} on ${DATE}.
 */

export default function baseStyles(topStyles, theme, palette) {
	
	const
		{ text, primary, accent, background } = palette
	
	return [ Styles.FlexColumn, Styles.FlexScale,Styles.PositionRelative, {
		
	} ]
}
